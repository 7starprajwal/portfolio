function NotFound() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "red",
        color: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "50px",
        fontWeight: "bold",
      }}
    >
      404 PAGE
    </div>
  );
}

export default NotFound;
